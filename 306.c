int integers[3];

bool check() {
}

bool solve(char* num, int state) {
        int flag = 0;
        char *p = num;

        switch(state) {
        case 0:
                while(*p) {
                        
                }
                break;
        case 1:
                break;
        case 2:
                break;
        default:
                break;
        }
}

bool isAdditiveNumber(char* num) {
       
}
